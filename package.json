{
  "name": "tutorial",
  "version": "1.0.0",
  "description": "test",
  "main": "tronbox-config.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate:shasta": "source .env && tronbox migrate --network shasta --reset -all",
    "compile": "tronbox compile -—compile-all"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "web3": "^1.2.11"
  }
}
